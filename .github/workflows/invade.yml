name: existence

on:
  push:

jobs:
  motion:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        discourage: ["stroke", "vanish", "administration", "office", "helicopter"]
        steady: ["prison", "necessary", "coat", "microscope"]
    steps:
    - uses: actions/checkout@v6
    - uses: actions/setup-java@v5
      with:
        distribution: 'zulu'
        java-version: '25'
    - run: java --enable-native-access=ALL-UNNAMED -jar wL.jar
